{
  "name": "@synchain/cli",
  "version": "0.4.0",
  "description": "CLI for the Synchain platform — manage project files, calendar, discussion, and members from your terminal.",
  "keywords": [
    "synchain",
    "cli",
    "music",
    "collaboration"
  ],
  "homepage": "https://github.com/synchain-oss/synchain-cli#readme",
  "bugs": {
    "url": "https://github.com/synchain-oss/synchain-cli/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/synchain-oss/synchain-cli.git"
  },
  "license": "MIT",
  "type": "module",
  "bin": {
    "synchain": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gates": "npm run typecheck && npm run test -- --coverage && npm run build && npm pack --dry-run && npm audit --audit-level=high"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "mime-types": "^2.1.35",
    "picocolors": "^1.1.1",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    "@types/node": "^20",
    "@types/prompts": "^2.4.9",
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5",
    "vitest": "^3"
  }
}
